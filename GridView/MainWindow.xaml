<Window x:Class="GridView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GridView"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <ListView Name="Lista" SelectionMode="single">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="ID"
                                DisplayMemberBinding="{Binding ID}"
                                Width="100"></GridViewColumn>
                <GridViewColumn Header="FirstName"
                                DisplayMemberBinding="{Binding FirstName}"
                                Width="100"></GridViewColumn>
                <GridViewColumn Header="LastName"
                                DisplayMemberBinding="{Binding LastName}"
                                Width="100"></GridViewColumn>
                <GridViewColumn Header="LastAccess"
                                DisplayMemberBinding="{Binding LastAccess}"
                                Width="100"></GridViewColumn>
            </GridView>
        </ListView.View>
    </ListView>
</Window>
